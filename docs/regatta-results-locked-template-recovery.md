# Regatta Results Story Locked Template Recovery

The locked Regatta Results Story template is maintained in:

- `regatta-results-story.js`
- `index.html` script cache tag for `regatta-results-story.js`

Locked baseline:

- Template name: `REGATTA RESULTS STORY - LOCKED TEMPLATE`
- Locked design commit: `af4f0e451ba3be53ea2563a8093b64868e22da5e`
- Clean interface commit: this document's commit

If the template is ever damaged or lost, restore `regatta-results-story.js` and the matching `index.html` script tag from the locked baseline in GitHub history. If saved browser drafts are lost, use any downloaded `*-full-studio-backup.json` file from Google Drive and ask Codex to restore it into the Studio.

Current user-facing Studio controls intentionally do not show backup/restore buttons. Recovery is handled from GitHub history or a saved backup file only when needed.
